<h1>Hello, Rails!</h1>

<%= link_to 'My Blog', controller: 'articles' %>

<%= form_for @article do |form| %>
    <%= form.cktext_area :text, :value => 'Default value', :id => 'sometext' %>
    <%= cktext_area :page, :title, :cols => 40, :ckeditor => {:uiColor => '#AADC6E', :toolbar => 'mini'} %>
<% end %>